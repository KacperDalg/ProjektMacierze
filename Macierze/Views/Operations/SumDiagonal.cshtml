@using Macierze.Models
@{

    ViewData["Title"] = "Suma przekątnej";
}
@*<p>tabela macierzy:
    @foreach(var number in Model.FormList){
        <span>@number</span>
    }
</p>
<p>@Model.MatrixSize</p>*@

<center><h1 style="font-size:30px;">Suma elementów głównej przekątnej macierzy jest równa: @ViewBag.Sum.</h1>
    <br />
    <br />
    <a asp-controller="Operations" asp-action="Options" class="btn btn-success" style="font-size:24px;position:absolute;bottom:50px;transform: translate(-50%, -50%);">Wróć do opcji</a>
</center>